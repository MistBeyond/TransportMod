# {TITLE} Docs Overview and Content Ownership

This document is the **index of the `{DIR}/` directory**: the overview of what each direct entry contains and the
authority for what content belongs in each of them. Read it before adding or moving documentation. In short, this
README is the **metadata of this documentation subtree**.

## Purpose

_TODO: one paragraph — what this subdirectory is for and how it relates to docs/._

## Overview

| Path | Purpose |
|------|---------|
| _TODO: one row per direct entry of this directory_ |

## Content Ownership

| Document | Belongs here | Does not belong here |
|----------|--------------|----------------------|
| _TODO: one row per document; what belongs vs what does not_ |

## Ownership Rules

_TODO: rules that apply to THIS directory only — its own content ownership and maintenance. Do NOT copy the parent
directory's rules (docs/README.md owns the global ones) and do NOT define what subdirectories may contain (each
subdirectory's own README owns that). Rules at different levels stay implicitly consistent, not explicitly copied._
